# commence

Delivery artifacts and skills for data products on Databricks.

- **`AGENTS.md`** — working context. Auto-loaded by Genie Code and Claude Code
- **`REPO-STRUCTURE.md`** — layout, file classification, exact destinations. Read before creating any file
- **Skill folders at root** — one per moment in the delivery lifecycle
- **`products/`** — one directory per data product; the specification is the hub
- **`capabilities/` · `incidents/` · `releases/` · `communications/` · `docs/`** — see `REPO-STRUCTURE.md`

The pipeline: BA material → requirements document → specification → work set → tickets → releases.
